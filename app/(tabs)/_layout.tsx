/**
 * Tabs layout stub — UI not built yet.
 * Minimal placeholder so the (tabs) route group resolves.
 */
import { Stack } from "expo-router";

export default function TabsLayout() {
  return <Stack />;
}
